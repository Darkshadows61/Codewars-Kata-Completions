//8kyu Gravity Flip
const flip = (d,a) => {
    if  (d === 'R') {
       arr = (a.sort((a,b) => a-b))
      return arr
    } else if 
    (d === 'L') {
       arr = (a.sort((a,b) => b-a))
      return arr
    }
}
//test flip('R', [3,2,1,2])